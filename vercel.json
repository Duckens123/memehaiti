{
  "version": 2,
  "builds": [
    { "src": "build.sh", "use": "@vercel/static-build", "config": { "zeroConfig": true } }
  ],
  "routes": [
    { "src": "/static/(.*)", "dest": "/static/$1" },
    { "src": "/(.*)", "dest": "/manage.py" }
  ]
}